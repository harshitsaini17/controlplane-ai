# Cascade cost simulation

**Simulation on the frozen synthetic corpus — not a production claim.** (06 §6.)

Generated by `python -m eval.cost_simulation`. Every figure below is either derived from the price table in `config/gateway.yaml` and the frozen corpus, or marked **NOT COMPUTED**. Nothing here is a placeholder.

## Provenance

| Field | Value |
|---|---|
| Dataset digest | `6a3ecbbe75fd020b…` |
| Frozen at | `f162959f7d29` — expected `6a3ecbbe75fd020b…` |
| Freeze gate | PASS |
| Code commit | `0f48172384db` |
| Reproducibility | 0f48172384db clean |
| Host load (start) | 0.77 / 1.12 / 1.27 · 12 CPUs — **QUIET** |

## Corpus

| Quantity | Value |
|---|---:|
| Frozen cases replayed | 280 |
| Estimated input tokens | 5,453 |
| Mean per case | 19.5 |

Token counts are **character-derived estimates** (`CHARS_PER_TOKEN = 4`, the same estimator the live `cost_budget` view uses), not tokenizer counts. They set the *scale* of the token split below; the headline percentage does not depend on them at all.

## The two tiers, and the ratio the saving is computed at

| Tier | Model | $/1K in | $/1K out |
|---|---|---:|---:|
| small | `openai/gpt-oss-20b` | 0.000075 | 0.000300 |
| frontier | `openai/gpt-oss-120b` | 0.000150 | 0.000600 |

**Measured tier ratio: 2.0x**, exact on input **and** output, so the ratio is **blend-independent** — no input/output mix can move it, and none was chosen to flatter it.

## Cost delta — a curve, because the routing fraction is not measured

`f` is the share of requests the cascade escalates to the frontier tier. An escalated request pays **both** tiers, so:

```
baseline(N) = N x C_frontier
cascade(N)  = N x C_small + f x N x C_frontier
saving      = 1 - (1/r + f)          r = 2.0
```

| `f` | small-tier tokens | frontier-tier tokens | cost delta |
|---:|---:|---:|---:|
| 0.00 | 5,453 | 0 | **+50.0%** (saves) |
| 0.10 | 5,453 | 545 | **+40.0%** (saves) |
| 0.25 | 5,453 | 1,363 | **+25.0%** (saves) |
| 0.50 | 5,453 | 2,726 | **+0.0%** (break-even) |
| 0.75 | 5,453 | 4,090 | **-25.0%** (COSTS) |
| 1.00 | 5,453 | 5,453 | **-50.0%** (COSTS) |

**Break-even at `f = 0.50`** — above that share of escalations the cascade costs more than sending everything to the frontier tier. Best case (`f = 0`) is **50.0%**, which is `1 - 1/r` and cannot be exceeded at this ratio.

**`f` itself is NOT COMPUTED.** ADR-009 escalates *on low confidence*, and the confidence signal that would pick the escalations does not exist in this build: `fast_consistency` is cut (SL-6), no code path reads `thresholds.tau_route`, and `audit_records.cascade_escalated` is a column nothing writes. A measured `f` needs a live cascade probe; until one ships, the row a reader should use is the one matching their own escalation rate.

**Cascade quality proxy (06 §6): NOT COMPUTED** — "fraction of small-tier answers whose fast-confidence cleared `tau_route`" is computed from the cut detector, so it has no input.

### Absolute dollars — input tokens only

Permitted for these two ids only (first-party prices, SL-3 as downgraded). **Input-side only**: the frozen corpus carries request text, not completions, so no measured `tokens_out` exists for it and an output figure would be invented.

| Path | input-token cost |
|---|---:|
| baseline — all 280 cases at frontier | $0.000818 |
| cascade at `f = 0` | $0.000409 |
| cascade at break-even `f = 0.50` | $0.000818 |

The figures are small because the corpus is 280 short cases; the **percentage** is the transferable quantity, which is why it is the headline.

## Ratio-parametric caveat (ADR-029, 06 §6)

Savings scale with **(tier ratio x routing fraction)**. The 2.0x above is *this deployment's* gap, and it sits at the **low end** of the industry range — at the flagship-vs-mini gaps published elsewhere the same routing saves proportionally more, because `1 - 1/r` rises with `r`.

The following is **context, cited to each vendor's own price page (retrieved 2026-08-27) — not our measurement**:

| Vendor | Pair | Gap |
|---|---|---|
| OpenAI | gpt-5.6-sol vs gpt-5-nano | 80x input / 50x output |
| Anthropic | Claude Opus 5 vs Haiku 4.5 | 5.0x on both |
| Anthropic | Claude Opus 5 vs Sonnet 5 | 2.5x — closest to ours |

The observed cross-vendor range is roughly **5x-50x+**, and it is wide because it depends entirely on which pair is chosen. That spread *is* the ratio-parametric point, not a caveat to it.

_Host load at end: 0.77 (1-min)._
