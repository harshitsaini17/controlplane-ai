"""Cost-plane detectors: `cost_budget`, `loop_guard`.

Implements 04 §2 registry rows (budget <1 ms) against the 05 §3 `cost_ledger`
table. Satisfies FR-DET-007. Cost is a signal + policy action, never a
special-cased subsystem (02 §5).
STUB(phase-1-scaffold): no implementation yet.
"""
