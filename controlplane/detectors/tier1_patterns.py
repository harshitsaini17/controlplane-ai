"""Tier-1 deterministic detectors: `tier1_pii`, `tier1_blocklist`.

Implements 04 §2 registry rows (budget <2 ms, span-accurate). Satisfies
FR-DET-001. Evidence fields never carry the raw matched value (NFR-SEC-001).
STUB(phase-1-scaffold): no implementation yet.
"""
