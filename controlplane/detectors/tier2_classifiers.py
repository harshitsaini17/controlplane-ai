"""Tier-2 classifiers: `tier2_injection` (input), `tier2_toxicity` (output).

Implements 04 §2 registry rows (budget <25 ms). Satisfies FR-DET-002.
STUB(phase-1-scaffold, Q-04 deferred): checkpoints chosen later via the latency
spike; interfaces only.
"""
