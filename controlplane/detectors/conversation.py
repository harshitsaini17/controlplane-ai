"""`conv_tracker` conversation-stage detector.

Implements the 04 §2 registry row (budget <1 ms): running per-conversation
totals emitting `conversation.cumulative_risk`. Satisfies FR-DET-006 (P2, Q-05).
STUB(phase-1-scaffold): no implementation yet.
"""
