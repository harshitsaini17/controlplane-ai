"""Span name constants.

Implements the fixed span vocabulary in 05 §5 (`cp.ingress`, `cp.input.tier1`,
... `cp.audit.write`), recorded as `latency_json` keys.
STUB(phase-1-scaffold): no implementation yet.
"""
