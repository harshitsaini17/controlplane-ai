"""Metrics registry.

Implements the fixed metric vocabulary in 05 §5 and serves 05 §2 `GET /metrics`.
Satisfies FR-OBS-001. `cp_gateway_overhead_ms` uses the normative definition in
06 §4 — never an ad-hoc variant.
STUB(phase-1-scaffold): no implementation yet.
"""
