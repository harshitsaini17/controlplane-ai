"""Policy store: load, validate, version and hot-reload policies/*.yaml.

Implements 04 §3 loading (wildcard expansion at load) against
`policy.schema.Policy`; serves 05 §2 `POST /admin/policies/reload` and
`GET /admin/policies`. Satisfies FR-CFG-001/002.
STUB(phase-1-scaffold): no implementation yet.
"""
