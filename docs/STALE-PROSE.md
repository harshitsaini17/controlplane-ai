# Stale prose register

Created 2026-08-30 under AGENTS.md §11.1 credit-conservation mode. Prose **outside**
judge-facing reports is listed here for one final pass instead of being fixed inline.

**Scope boundary, because it is the whole point of the file:** prose *inside* a judge-facing
report is NOT listed here — it is a published claim and gets fixed where it is found. The two
entries below are therefore *not* stale prose; they are **stale artifacts awaiting a
measurement run**, recorded here only so the distinction is written down somewhere.

## Awaiting a measurement run (not prose — do not "fix" by editing)

| Artifact | What is stale | Regenerated by |
|---|---|---|
| `reports/eval_report.md` (~line 119) | The `entity_enricher` row still reads `"not implemented — stub; needs spaCy en_core_web_sm (ADR-011)"`. The stage landed 2026-08-29 and `eval/run_all.py` now derives that string by probing the host (`_enricher_reason()`), so the code is correct and only the artifact predates it | `python -m eval.run_all` on a quiet host |
| `reports/latency_report.md` | Predates both the `tier2_injection` landing and the partitioned absent list (M-42/M-43 fixes) | `python -m eval.bench_latency` on a quiet host |

Editing either by hand would substitute a typed value for a measured one — AGENTS.md §5.4.

## Stale prose (the actual list)

**Empty as of 2026-08-30.** Stated explicitly rather than left blank: nothing has been
*found* and deferred. Every doc figure is currently re-derived clean by
`eval.check_derivations` (55/55 OK), so the known-stale surface is the two artifacts above.
Add rows here as prose is found during other work — that is what the file is for.
